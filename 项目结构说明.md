# 项目结构说明

> 生成于 2026-08-21，合并「htade」「spytrend」两个逻辑项目后统一梳理。

## 一句话

`美股投资洞察分析` 是一个**单一 git 仓库**（`main` 分支，remote: `github.com/y840110/us-stock-insight`），
原「htade」「spytrend」只是它 `exp9/` 下的两个子目录，现已从项目索引中合并进来。

## 模块全景

```
美股投资洞察分析/                      # 顶层 git 仓库
│
├── TradingAgents/                   # 【AI 分析框架 · 核心】
│   ├── fintech/
│   │   ├── p1/                      # 数据获取（Chrome CDP → Yahoo Finance）
│   │   ├── p2/                      # 宏观/市场状态分析（多 agent 辩论）
│   │   ├── p3/                      # 板块轮动分析
│   │   └── p4/                      # 选股策略
│   ├── agents/                      # 多智能体（CDO/CRO 等）
│   ├── strategies/                  # 交易策略
│   ├── spy_ml/                      # SPY 机器学习
│   └── 短线交易/                     # 短线交易实验
│
├── exp9/                            # 【策略实验室 · 实验代码】
│   ├── live_trading/                # ← 原「htade」项目
│   │   ├── engine/                  #   引擎 engine_h3_4_1.py（胜率60% RR1.47）
│   │   ├── combat/                  #   实盘战斗（message_builder / schema / daily_scheduler）
│   │   ├── backtest/                #   回测
│   │   ├── kline_scanner/           #   K线扫描器
│   │   ├── htrade/                  #   日志/结果
│   │   └── scripts/                 #   辅助脚本
│   ├── spytrend/                    # ← 原「spytrend」项目
│   │   └── v2 ~ v6 backtest         #   SPY EMA20/50 趋势跟踪（v6 最佳 $27,463）
│   ├── dtrade/                      # 日内交易实验脚本（swing/HMM/regime）
│   ├── model/                       # 价格行为学专家模型（p02/p18 等）
│   ├── backtest/                    # 回测结果
│   ├── 经验总结/                     # 课程经验文档
│   └── learning_plan.json           # 课程学习计划
│
├── backtest_lab/                    # 【回测实验室】SQLite（backtest.db）
├── scripts/                         # 脚本（当前为空）
├── 中间过程/                         # 中间数据（klines 等）
├── reports/  docs/  memory/         # 报告 / 文档 / 项目记忆
└── 方方土价格行为学_新/               # 学习视频 / 字幕 / 笔记
```

## 关键约定

1. **物理目录不动**：以下 cron 任务硬编码了绝对路径，任何移动都会弄断它们：
   - `hourly_1h_kline_update` → `.../美股投资洞察分析/TradingAgents/fintech/p1/fetch_us_stocks_cdp.py`
   - `daily_scheduler_mon-fri` → `.../美股投资洞察分析/exp9/live_trading/combat/scripts/daily_scheduler.py`

2. **数据铁律**：交易数据必须来自真实数据源，禁止编造。

3. **K线铁律**：禁止以数组位置取价格，必须以 `time` 字段为准。

## 模块职责速查

| 模块 | 职责 | 关键入口 |
|------|------|----------|
| TradingAgents | AI 分析框架（数据→宏观→板块→选股） | `fintech/p1/p1_main.py` |
| exp9/live_trading | 日内交易引擎（实盘） | `engine/engine_h3_4_1.py` |
| exp9/spytrend | 波段趋势策略（回测） | `spytrend/v6_backtest.py` |
| exp9/dtrade | 日内交易实验 | `dtrade/spy_swing_scanner.py` |
| exp9/model | 价格行为学专家模型 | `model/unified_engine.py` |
| backtest_lab | 回测记录管理 | `backtest_lab/sweep.py` |
